import { GenericContainerArray } from './generic-container-array';

describe('GenericContainerArray', () => {
  it('should create an instance', () => {
    expect(new GenericContainerArray()).toBeTruthy();
  });
});
